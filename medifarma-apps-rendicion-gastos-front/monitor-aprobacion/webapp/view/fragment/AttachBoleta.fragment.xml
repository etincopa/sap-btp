<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout" xmlns:tnt="sap.tnt" xmlns:f="sap.ui.layout.form"
	xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m">
	<Dialog draggable="true" resizable="false" class="sapUiSizeCompact" title="Visualizar Archivos" afterClose="onCloseDialogAttach">
		<content>
			<UploadCollection
			noDataDescription="{i18n>txtnoDataDescription}"
			noDataText="{i18n>txtNoDataText}"
			maximumFilenameLength="{Config>/maximumFilenameLength}" 
			maximumFileSize="{Config>/maximumFileSize}" 
			multiple="{Config>/multiple}"
			busy="{Config>/busy}"
			change="onChangeBoleta"
			typeMissmatch="onTypeMissmatch"
			fileType="{Config>/fileTypes}"
			uploadComplete="uploadComplete"
			uploadTerminated="onUploadTerminated"
			instantUpload="true"
			items="{ path: 'DocumentosBoleta>/' }"
			uploadEnabled="{Config>/uploadEnabled}"
			uploadButtonInvisible="true" 
			mode="{Config>/mode}">
				<items>
					<UploadCollectionItem 
					documentId="{DocumentosBoleta>LoioId}"
					fileName="{DocumentosBoleta>Descript}.{DocumentosBoleta>DocType}"
					mimeType="{DocumentosBoleta>Mimetype}"
					url="{DocumentosBoleta>LoioId}"
					press=".onPressItemDownload"
					visibleDelete="false"
					visibleEdit="false">
					</UploadCollectionItem>
				</items>
			</UploadCollection>
		</content>
		<beginButton>
			<Button text="Cerrar" press="onCloseDialogAttach" icon="sap-icon://decline" type="Accept"/>
		</beginButton>
	</Dialog>
</core:FragmentDefinition>