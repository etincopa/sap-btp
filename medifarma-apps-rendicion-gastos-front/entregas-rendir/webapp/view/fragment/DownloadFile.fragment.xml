<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout" xmlns:tnt="sap.tnt" xmlns:f="sap.ui.layout.form"
	xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m">
	<Dialog draggable="true" resizable="false" class="sapUiSizeCompact" title="Adjuntar Archivos" afterClose="onCloseDialogAttach">
		<content>
			<UploadCollection
			noDataDescription="{i18n>txtnoDataDescriptionDownload}"
			noDataText="{i18n>txtNoDataTextDownload}"
			maximumFilenameLength="{Config>/maximumFilenameLength}" 
			maximumFileSize="{Config>/maximumFileSize}" 
			multiple="{Config>/multiple}"
			busy="{Config>/busy}"
			change="onChange"
			typeMissmatch="onTypeMissmatch"
			fileType="{Config>/fileTypes}"
			uploadComplete="uploadComplete"
			uploadTerminated="onUploadTerminated"
		
			instantUpload="true"
			items="{ path: '/' }"
			uploadEnabled="{Config>/uploadEnabled}"
			uploadButtonInvisible="true" 
			mode="{Config>/mode}">
				<toolbar>
					<OverflowToolbar>
						<Title id="attachmentTitleDownload" text="Adjuntos ({= ${/}.length })"/>
						<ToolbarSpacer/>
						<!--<UploadCollectionToolbarPlaceholder/>-->
					</OverflowToolbar>
				</toolbar>
				<items>
					<UploadCollectionItem 
					documentId="{Documents>LoioId}"
					fileName="{name}"
					mimeType="{mimeType}"
					url="{url}"
					deletePress="onFileDeleted"
					press=".onPressItemDownload"
					enableEdit="false"
					enableDelete="false" 
					visibleDelete="false"
					visibleEdit="false">
					</UploadCollectionItem>
				</items>
			</UploadCollection>
		</content>
		<beginButton>
			<Button text="Cerrar" press="onCloseDialogDownload" icon="sap-icon://decline" type="Accept"/>
		</beginButton>
	</Dialog>
</core:FragmentDefinition>
